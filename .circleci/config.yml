version: 2.1

# Define the jobs we want to run for this project
jobs:
  build:
    docker:
      - image: alpine
    steps:
      - run: docker login -umilan.jecmenica@gmail.com totalbot123demo.jfrog.io -p${JFROG_DOCKER_API_KEY}

# Orchestrate our job run sequence
workflows:
  build_and_test:
    jobs:
      - build
